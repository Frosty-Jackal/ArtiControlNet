# 依赖与构建产物
node_modules/
dist/

# 密钥与本地配置（切勿提交）
.env
Server/.env
*.env.local

# 运行时临时数据
Server/storage/
tmp_images/

# 个人作品库（Spec5 持久目录，重启不清、不受 TTL 影响）
Server/gallery/

# 社区帖子图片（Spec9 持久目录，与 gallery/ 分开）
Server/community/

# 本地认证数据库（含 WAL/SHM 伴生文件）
Server/artcn.db
Server/artcn.db-wal
Server/artcn.db-shm

# 数据库备份（升级改表前的快照，含账号/邮箱/密码哈希，仅本地留存）
# 命名见 README「部署到云服务器」：cp Server/artcn.db Server/artcn.db.bak-$(date +%F)
Server/artcn.db.bak*

# 收款码（Spec19：含个人收款二维码与实名，仅本地留存）
Server/payment.jpg

# 本地 API 手册（含真实密钥，仅本地参考）
API's Usage/

# 个人面经（仅本地留存，勿提交）
产品面经.txt

# AI 工具配置（含内部架构、模型型号与 Spec 索引，仅本地留存）
CLAUDE.md

# 宣传素材截图（仅本地留存，勿提交）
AD/
赋能设计的AIGC系统/

# 内容运营（仅本地留存，勿提交）
内容运营/

# 个人作品集打包（仅本地留存，勿提交）
作品集.zip

# Python
__pycache__/
*.py[cod]
.venv/
venv/
